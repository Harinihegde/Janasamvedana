# Exact versions used to produce the reported results.
numpy==2.3.0
pandas==2.3.0
scipy==1.15.3
scikit-learn==1.7.0        # pinned: unpickling the trained models requires a matching version
opencv-python==4.13.0.92
ultralytics==8.4.56        # YOLO person detection
torch==2.7.1               # backs ultralytics + torchvision NMS
torchvision==0.22.1
matplotlib==3.10.3

# optional — only for scripts in experiments_reverted/ and the optimisation sweep
xgboost==3.3.0
imbalanced-learn==0.14.2
